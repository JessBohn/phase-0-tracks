// Jessica Bohn
// 06/20/16
// 7.3 - Solo Challenge

// Release 0

  // Input list of different phrases
  // Calculate number of characters in each phrase
  // Set variable for number of characters in string, numCharacters, to zero
  // Set index variable, i, to zero
    // FOR i less than the length of the array
      // IF numCharacters is less than the length of string in array at i
        // Set numCharacters to length of string in array at i
      // END
      // Increase value of i by 1
    // ENDFOR
  // Print string at index of value i where numCharacters is max for the set